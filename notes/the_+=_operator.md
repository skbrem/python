Increasing the value of a a variable is very common in python scripting. 
To make it easier, there is a common shorthand notation that achieves the same results as adding two variables together. 

## Example

For example,

instead of using `sum = sum + number`

Rather use: "sum += number"
